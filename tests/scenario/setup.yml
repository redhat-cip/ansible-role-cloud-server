---
- name: Create SSH Key
  include_role:
    name: cloud-sshkey
  vars:
    cloud_sshkey_name: '{{ cloud_server_sshkey }}'

- name: Create Security Group
  include_role:
    name: cloud-securitygroup
  vars:
    cloud_securitygroup_name: '{{ cloud_server_security_group }}'
